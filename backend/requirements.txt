# Web API (Python 3.12+)
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
pydantic>=2.10.0
sqlalchemy>=2.0.36

# Testing
pytest>=8.3.0
httpx>=0.28.0
# Audio / MIDI
numpy>=2.1.0
librosa>=0.10.2
soundfile>=0.13.0
pretty_midi>=0.2.10

# Basic Pitch runtime (ONNX on Windows — no TensorFlow on Py3.12+)
onnxruntime>=1.20.0
mir-eval>=0.7
resampy>=0.4.3
scikit-learn>=1.5.0
scipy>=1.14.0
typing-extensions>=4.12.0

# Stem separation (PyTorch)
torch>=2.5.0
torchaudio>=2.5.0
demucs>=4.0.1

# basic-pitch pins tensorflow on py>=3.11; skip its deps (listed above).
--no-deps
basic-pitch==0.4.0
